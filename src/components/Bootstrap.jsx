import { Alert, Button } from "react-bootstrap";
import React from "react";

function Bootstrap() {
  return (
    <>
      <Alert variant="secondary">Form successfully submitted</Alert>
      <Button variant="dark">Submit</Button>
    </>
  );
}

export default Bootstrap;
